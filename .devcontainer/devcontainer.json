
{
  "name": "SonarQube Dev Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [9000],
  "postCreateCommand": "docker-compose up -d",
  "features": {
    "docker-in-docker": "true"
  }
